# lazygit 日常使用指南（写给初学者）

> 目标：不追求炫技，读完就能把 lazygit 当主力 git 工具用。
> 全文讲法：大白话 + 图 + 例子。所有按键都可以 `cd` 到任意一个 git 仓库里照着敲。
> 本机已装好 lazygit v0.65.1（路径 `~/.local/bin/lazygit`），跳过安装可直接从第 3 节开练。

---

## 目录

1. [先想明白：为什么学 lazygit](#1-先想明白为什么学-lazygit)
2. [安装：各个平台的装法](#2-安装各个平台的装法)
3. [第一性原理：面板 + 焦点（这是 lazygit 最大的坎）](#3-第一性原理面板--焦点这是-lazygit-最大的坎)
4. [第 1 天必会：打开、逛、退](#4-第-1-天必会打开逛退)
5. [日常主干：暂存 → 提交 → 推送](#5-日常主干暂存--提交--推送)
6. [保命须知：d 是危险键，z 是后悔药](#6-保命须知d-是危险键z-是后悔药)
7. [分支操作：新建、切换、合并、删除](#7-分支操作新建切换合并删除)
8. [看历史：commits 面板](#8-看历史commits-面板)
9. [进阶：改历史、stash、搜索](#9-进阶改历史stash搜索)
10. [配置：改键位和主题](#10-配置改键位和主题)
11. [速查表（打印出来贴墙上）](#11-速查表)
12. [7 天练习路线](#12-7-天练习路线)

---

## 1. 先想明白：为什么学 lazygit

一句话：**lazygit 把 git 命令行变成了一个「所见即所得」的终端图形界面，git 用得越多，它省的手越多。**

- 平时改一次代码要敲一串：`git status` → `git diff` → `git add xxx` → `git commit -m "..."` → `git push`。lazygit 里这些全是**按一个键**，而且结果立刻显示在屏幕上。
- 它最大的价值在**复杂操作**：暂存到行级别（`git add -p` 的图形版）、交互式 rebase、cherry-pick、解决冲突——命令行里容易手抖的操作，它给你弹窗确认 + 可视化。
- **它不是 git 的替代品，是遥控器**：lazygit 干的每件事最终都在执行真 git 命令。你随时可以按 `q` 退出，用 `git log` 一看，什么都在、什么都没弄乱。这是新手敢于大胆用它的底气。

**给你的两个建议（能少走 80% 弯路）：**

1. 迷路了先按 `?`——lazygit 自带说明书，**当前焦点在哪，就显示哪的快捷键**。本文只需要读一遍，键位忘了永远先按 `?`。
2. 前两天刻意只用 lazygit 完成日常提交，别一紧张就回命令行。按错最多弹个确认框，按 `esc` 就取消，坏不了。

---

## 2. 安装：各个平台的装法

lazygit 是单个二进制文件，不挑环境。按平台挑一条：

**Ubuntu 22.04（apt 源里没有，用官方二进制，本文机器就是这么装的）：**

```bash
LAZYGIT_VERSION=$(curl -s "https://api.github.com/repos/jesseduffield/lazygit/releases/latest" \
  | grep -Po '"tag_name": *"v\K[^"]*')
curl -Lo lazygit.tar.gz \
  "https://github.com/jesseduffield/lazygit/releases/download/v${LAZYGIT_VERSION}/lazygit_${LAZYGIT_VERSION}_Linux_x86_64.tar.gz"
tar xf lazygit.tar.gz lazygit
sudo install lazygit -D -t /usr/local/bin/    # 需要 sudo 密码；没有的话装到 ~/.local/bin 也行
rm -f lazygit lazygit.tar.gz
```

**没有 sudo 权限**（本文机器的装法）：把上面最后一行换成 `install -D lazygit -t ~/.local/bin/`，`~/.local/bin` 通常已在 PATH 里。

**其他平台一句话：**

| 平台 | 命令 |
|---|---|
| Ubuntu 23.04+ / Debian 13+ | `sudo apt install lazygit` |
| macOS | `brew install lazygit` |
| 有 snap 的机器 | `sudo snap install lazygit` |
| Windows | `scoop install lazygit` 或 `choco install lazygit` |

装完验证：

```bash
lazygit --version    # 打印版本号即成功
```

---

## 3. 第一性原理：面板 + 焦点（这是 lazygit 最大的坎）

新手 90% 的懵来自一件事：**屏幕上东西太多，不知道自己在对谁按键。**

记住一个心法就通了：**你的「焦点」在哪个面板，按键就只对那个面板里光标选中的东西生效。**

打开 lazygit，屏幕长这样：

```
┌───────────────┬──────────────────────────────────┐
│ 1 状态 Status │                                  │
│───────────────│                                  │
│ 2 文件 Files  │         主区（大窗口）              │
│───────────────│                                  │
│ 3 分支 Branch │   左边焦点选了谁，这里就显示谁的详情： │
│───────────────│   diff / 提交内容 / 图形化历史…      │
│ 4 提交 Commit │                                  │
│───────────────│                                  │
│ 5 收藏 Stash  │                                  │
└───────────────┴──────────────────────────────────┘
   左侧 5 个小面板（列表）        右侧主区（详情）
```

移动焦点只靠三组键：

| 按键 | 作用 | 记法 |
|---|---|---|
| `1` `2` `3` `4` `5` | **直达**对应面板（状态/文件/分支/提交/收藏） | 面板从上到下正好是 1–5 |
| `h` / `l`（或 ←/→） | 焦点在左边面板和右边主区之间**左右切** | vim 同款，见 [vim-usage.md](vim-usage.md) |
| `j` / `k`（或 ↑/↓） | 在当前列表里**上下**移动光标 | vim 同款 |
| `tab` | 在面板内部的「页签」间切换 | 如文件面板的「未暂存/已暂存」两栏 |

**和 Vim 完全同构的句子公式：**

```
   ┌─────────┐     ┌─────────┐     ┌──────────┐
   │ 选面板   │  →  │ 选对象   │  →  │ 按动作键  │
   │ 1-5 h/l │     │ j/k     │     │ space 等 │
   └─────────┘     └─────────┘     └──────────┘
    对哪类东西动手    具体哪一个       做什么动作
```

比如「把 `main.go` 加进暂存区」翻译成 lazygit 句子就是：`2`（文件面板）→ `j j`（光标移到 main.go）→ `space`（暂存）。想通了这一点，后面几十个按键根本不用背——**先选对象，再按动作**。

**三条铁律：**

1. **迷路了先按 `?`**，当前面板的全部快捷键就在眼前，按 `esc` 关掉。
2. **想退出就按 `q`**（回上级界面），想取消刚才的弹窗按 `esc`，任何时候都不会被卡住。
3. **删除类按键（`d`、`D`）按下前看清弹窗**，手滑了赶紧 `esc`。

---

## 4. 第 1 天必会：打开、逛、退

```bash
cd 你的git仓库        # 必须先进入一个 git 仓库，否则提示 not a git repository
lazygit              # 敲这一个词就进来了
```

进去了以后，第一天只需要会这一套（「逛」就是用 j/k 把五个面板都走一遍）：

| 按键 | 意思 | 相当于 |
|---|---|---|
| `lazygit` | 打开 | — |
| `1`–`5` | 逛五个面板 | `git status` / `git branch` / `git log` … |
| `j` / `k` | 列表里上下移动 | — |
| `h` / `l` | 左右切「列表 ↔ 详情」 | — |
| `enter` | 展开 / 查看详情 | `git diff`、`git show` |
| `PgUp` / `PgDn`（或 `Ctrl+u`/`Ctrl+d`） | 主区太长时翻页 | — |
| `q` | 退回上级界面 / 退出 lazygit | — |
| `esc` | 取消弹窗、收起搜索 | — |
| `?` | 看当前面板的快捷键说明书 | — |

> 实例：看看自己刚才改了什么
> `2`（文件面板）→ `j` 选中某个文件 → 主区立刻显示 diff。第一天会这些就能「逛」了。
> 逛完按 `q` 退出，再用 `git status` 验证一下——什么都没变，放心。

---

## 5. 日常主干：暂存 → 提交 → 推送

这是每天重复几十遍的动作，全部在 **2 号文件面板**完成：

| 按键 | 作用 | 相当于 |
|---|---|---|
| `space` | 暂存 / 取消暂存当前文件 | `git add 文件` / `git restore --staged 文件` |
| `a` | 全部暂存 / 全部取消 | `git add -A` |
| `tab` | 在「未暂存 / 已暂存」两栏之间切焦点 | — |
| `c` | 提交（弹出输入框写 message，回车确认） | `git commit` |
| `C` | 用外部编辑器写 commit message | `git commit`（不用 `-m`） |
| `A` | 把暂存的改动**合进上一次提交** | `git commit --amend` |
| `P` | 推送（大写） | `git push` |
| `p` | 拉取（小写） | `git pull` |
| `e` | 用 `$EDITOR` 编辑文件 | `vim 文件`（本机就是 vim，见 [vim-usage.md](vim-usage.md)） |
| `o` | 用系统默认程序打开文件 | `xdg-open 文件` |
| `i` | 把文件加进 `.gitignore` | — |
| `s` | 把当前改动存进 stash（见第 9 节） | `git stash` |

> **推荐的主干流程**（练熟后 10 秒一次提交）：
> `lazygit` → `2` → `j` 选文件 → 瞄一眼右侧 diff → `space` 暂存 → `c` 写 message → 回车 → `P` 推送。
> 注意两点：① 提交完推送用**大写** `P`；② 忘暂存就按 `c`，lazygit 会问你「要不要把所有改动都暂存再提交」，新手直接选 yes 即可。

### 5.1 杀器：暂存到「块」和「行」（比 git add -p 好用十倍）

命令行里做局部暂存要跟 `git add -p` 的 y/n 对话搏斗，lazygit 里就是「看着选」：

1. `2` 进文件面板，光标停在某个文件上（先别按 enter）
2. **直接按 `space`** → 只暂存光标所在的那个**代码块（hunk）**，主区实时高亮
3. 按一次 `enter` 进 diff 视图 → 按 `v` 开始行选择 → `j/k` 拉出想要的行 → `space`
   → 只暂存**选中的那几行**
4. 提交时 lazygit 自动只提交暂存区内容，剩下的改动原地不动

> 典型场景：一个文件里混了「bug 修复」和「顺手格式化」两类改动，想拆成两个提交——就是这一节的用武之地。

---

## 6. 保命须知：d 是危险键，z 是后悔药

**`d`（小写）= 删除类动作**，在不同面板删的东西不一样，但它删的通常是「真东西」：

| 在哪个面板 | `d` 干什么 | 能反悔吗 |
|---|---|---|
| 2 文件面板 | **丢弃**工作区改动（改没的代码真没了） | ❌ 救不回来 |
| 3 分支面板 | 删除分支 | 已合并的一般可重建；未合并会弹窗警告 |
| 4 提交面板 | 丢弃这个提交 | ✅ `z` 可撤销 |
| 5 收藏面板 | 删除 stash | ❌ 救不回来 |

所以 lazygit 对 `d` **都会弹确认框**——红字警告的弹窗，读一遍再按确认，不想要就 `esc`。

**`z`（小写）= 撤销（后悔药）**，`Ctrl+z` = 重做：

- `z` 撤销的是「改变了 git 历史 / 分支状态」的操作：提交、rebase、合并、reset、删分支……底层走的是 reflog，非常可靠。
- ⚠️ 注意边界：**`d` 丢掉的工作区改动不在后悔药范围内**（git 本身也没存过它们）。真正没底的「丢工作区代码」操作，宁可先 `s` 存个 stash 再丢弃。

> **记住这两句：`d` 之前看弹窗，出事之后按 `z`。**
> 另外还有个大写 `D`（文件面板）= 打开 reset 选项菜单（mixed/hard 重置到某提交），新手阶段用不到 `d` 就够了。

---

## 7. 分支操作：新建、切换、合并、删除

全部在 **3 号分支面板**。`tab` 可以在 Local（本地）/ Remote（远程）/ Tags 三个页签间切：

| 按键 | 作用 | 相当于 |
|---|---|---|
| `n` | 新建分支（输入名字，基于当前所在分支创建） | `git switch -c xxx` |
| `space` | 检出（切换到）光标所在分支 | `git switch xxx` |
| `M` | 把光标所在分支**合并进**当前分支 | `git merge xxx` |
| `r` | 把当前分支**变基到**光标所在分支之上 | `git rebase xxx` |
| `f` | 快进当前分支到其远程状态 | `git pull --ff-only` |
| `d` | 删除分支（有确认弹窗） | `git branch -d` |
| `T` | 给当前提交打标签 | `git tag` |
| `enter` | 看这个分支的提交列表 | — |
| `g` | 打开 reset 选项菜单（当前分支重置到所选分支） | `git reset` |

> 实例：拉了个需求分支开发，写完合回主干
> `3` → `n` 输入 `feat/xxx` → 在新分支上照第 5 节提交 → `3` → `j` 选中 `main` → `space` 切回 main → `M` 把 feat/xxx 合进来 → `P` 推送。
> 中间出冲突？看下面这段。

### 遇到冲突怎么办

合并/变基发生冲突时，2 号文件面板会列出冲突文件，光标停上去按 `enter`，主区进入**冲突解决视图**：

```
<<<<<<< HEAD        ← 当前分支的内容
你的代码
=======             ← 分界线
别人的代码
>>>>>>> feat/xxx
```

- `←` / `→`：在「保留我的 / 保留对方的」两块之间切换选择
- `↑` / `↓`：跳到上一个 / 下一个冲突
- `space`：采用当前选中的那块
- 不想手动抠：按 `M` 直接调起外部合并工具；实在想跑路，开菜单 `x` 选 abort 放弃本次合并
- 全部解决完，lazygit 自动继续合并流程

---

## 8. 看历史：commits 面板

**4 号提交面板**就是一份带图形分支线的 `git log`：

| 按键 | 作用 | 相当于 |
|---|---|---|
| `enter` | 看这个提交改了哪些文件（再按 enter 看 diff） | `git show` |
| `space` | 检出这个提交（**detached HEAD**，新手慎用，看完 `3` 面板切回分支即可） | `git checkout <sha>` |
| `/` | 搜索提交信息（`n`/`N` 跳下一个/上一个，`esc` 清除） | — |
| `c` | 标记这个提交为待 cherry-pick（见第 9 节） | — |

> 实例：查「上周那个改配置的提交叫啥」
> `4` → `/` 输入关键词（比如 `config`）→ `n` 在结果里跳 → 找到后 `enter` 看改了哪些文件。

---

## 9. 进阶：改历史、stash、搜索

### 9.1 交互式 rebase：合并、改写、重排提交（lazygit 最强的地方）

命令行里交互式 rebase 要对着一个 vim 编辑器改 pick/squash 清单，lazygit 里直接**对提交列表按键**：

1. `4` 进提交面板，把光标停在**想从哪开始改**的那个提交上（它会连同它上面的历史一起进入编辑）
2. 按 `e` 开始编辑——此时顶部提交列表出现「pending」标记
3. 对列表里的提交按：

| 按键 | 作用 | 大白话 |
|---|---|---|
| `r` | 改写提交信息 | 「这条 message 打错了，重写」 |
| `s` | squash：把这条**压进它下面那条** | 「这两条其实是一件事」 |
| `f` | fixup：同 squash，但**丢掉**这条的 message | 「这条只是补充，别留废话」 |
| `d` | 丢弃这个提交 | 「这条提交不要了」 |
| `Ctrl+j` / `Ctrl+k` | 把这条提交**下移 / 上移**（重排顺序） | 「先修 bug 再加功能」 |

4. 弄完按 `m` 继续，rebase 执行完收工；中途后悔开菜单 `x` 选 abort 全部放弃

> ⚠️ 铁律：**已经推送到公共分支的提交别改**。改完推送时 lazygit 会要求 force push，团队共享的分支这么做等于毁别人工作区。只对「还在自己手里没推（或只推给自己独占分支）」的提交做这套操作。

### 9.2 stash：手里活先存一存

场景：写到一半，线上出 bug 要切走——代码还没法提交，怎么办？

1. `2` 文件面板按 `s`：当前改动整体存入 stash（相当于 `git stash`），工作区立刻干净
2. 放心切分支、修 bug
3. 回来后 `5` 进收藏面板：

| 按键 | 作用 | 相当于 |
|---|---|---|
| `space` | 应用这个 stash（**保留** stash 备份） | `git stash apply` |
| `g` | 应用并**删除**这个 stash（pop） | `git stash pop` |
| `d` | 删除（真没了，看清弹窗） | `git stash drop` |

### 9.3 cherry-pick：把一个提交「搬」到另一个分支

1. `4` 提交面板，光标停在要搬的提交上按 `c`（标记为「已复制」）
2. `3` 分支面板，`space` 切换到目标分支
3. 按 `v` 粘贴——这个提交（及其改动）就复制过来了

### 9.4 万能后门：临时敲 shell 命令

按 `:` 可以直接敲一条 shell 命令（比如 `git status`），输出显示在命令日志里。懒人福音：基本可以不退出 lazygit 干所有事。

---

## 10. 配置：改键位和主题

配置文件在 `~/.config/lazygit/config.yml`（没有就新建）。最方便的入口：`1` 状态面板里按 `e` 直接编辑。

一份新手够用的起步配置（每行都有注释）：

```yaml
gui:
  showRandomTip: false        # 关掉每次启动的随机小贴士（有点吵）
  border: rounded             # 边框圆角，好看
  theme:
    activeBorderColor:        # 焦点所在面板的边框色（一眼看出自己在哪）
      - green
    selectedLineBgColor:
      - reverse               # 光标行反色高亮

# 不满意默认按键？在这里覆盖，比如把「提交」换回大写习惯：
keybinding:
  commits:
    commit: "c"               # 默认就是这个，改成你顺手的即可
```

改完**重启 lazygit** 生效（vim 里有 `:source`，lazygit 没有，关了重开就好）。

> 想查所有可配置项：`?` 看到的每个按键都对应配置文件里 `keybinding` 下的一个字段；官方默认配置完整版见仓库里的 `docs/Config.md`。
> 偷懒办法：什么都不配，默认键位已经够用一年。

---

## 11. 速查表

### 救命三键

```
?          —— 迷路了看说明书（当前面板的全部按键）
q / esc    —— 退出 / 取消
z          —— 撤销（Ctrl+z 重做）
```

### 导航（第一性原理）

```
1 2 3 4 5   直达五个面板     j / k     列表上下移动
h / l       列表↔详情左右切   tab       面板内页签切换
enter       展开看详情       ?         当前面板快捷键
```

### 文件面板（2，日常主干）

```
space       暂存/取消暂存     a         全部暂存/取消
tab         未暂存↔已暂存    c         提交
A           合进上次提交      C         编辑器写提交信息
d           丢弃改动(危险!)   D         reset 选项菜单
e           编辑文件         i         加进 .gitignore
s           存 stash        v+space    行级暂存
```

### 分支面板（3）

```
n           新建分支          space     检出/切换
M           合并进当前分支     r         变基到所选分支
f           快进              d         删分支
T           打标签            g         reset 选项
```

### 提交面板（4）

```
enter       看提交详情        /         搜索提交
r           改写信息          s         squash 压进下一条
f           fixup 丢 message  d         丢弃提交
e           从这里开始改历史   Ctrl+j/k  上移/下移重排
m           继续 rebase       c/v       cherry-pick 复制/粘贴
```

### 全局动作

```
P           推送              p         拉取
z / Ctrl+z  撤销 / 重做       x         打开动作菜单
:           敲 shell 命令      Ctrl+r    切换最近仓库
```

---

## 12. 7 天练习路线

| 天 | 任务 | 达标标准 |
|---|---|---|
| 1 | 找个玩具仓库 `lazygit` 打开逛一圈；用第 4 节 | 能用 1–5 逛完五个面板，知道 h/l j/k 在干嘛 |
| 2 | 只用 lazygit 完成一次完整提交 | space → c → P 一条龙不回命令行 |
| 3 | 练 hunk / 行级暂存，把混在一起的改动拆成两个提交 | 能用 enter + v + space 拆干净 |
| 4 | 练分支：n 建分支 → 提交 → space 切回 main → M 合并 | 全程不敲 git 命令 |
| 5 | 练 z 撤销：提交后 z 撤掉、再 Ctrl+z 重做；故意按 d 看 | 知道 z 能救什么、救不了什么 |
| 6 | 练 rebase 三件套：r 改 message、s 合并提交、Ctrl+j/k 重排 | 能把 3 条乱提交整理成 1 条干净的 |
| 7 | 综合演习：stash 存活 → 切分支修 bug → 回来 pop → 继续提交 | 中断工作不丢东西 |

之后就是日常使用自然变强。遇到不会的操作，**记住那把万能钥匙：按 `?` 现场查**——lazygit 把说明书内置在了每个面板里，这是它对新最友好的地方。

---

*生成于 2026-09-22，基于 lazygit v0.65.1（本机 `~/.local/bin/lazygit` 实测安装）；键位以 lazygit 内置帮助（`?`）与官方文档为准。*
