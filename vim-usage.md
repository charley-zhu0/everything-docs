# Vim 日常使用指南（写给初学者）

> 目标：不追求炫技，读完就能把 Vim 当主力编辑器用。
> 全文讲法：大白话 + 图 + 例子。所有例子都可以直接开个测试文件照着敲。

---

## 目录

1. [先想明白：为什么学 Vim](#1-先想明白为什么学-vim)
2. [第一性原理：模式](#2-第一性原理模式这是-vim-最大的坎)
3. [第 1 天必会：打开、改、存、退](#3-第-1-天必会打开改存退)
4. [移动：先把路走熟](#4-移动先把路走熟)
5. [编辑：增删改复制粘贴](#5-编辑增删改复制粘贴)
6. [查找与替换](#6-查找与替换)
7. [核心心法：动词 + 数字 + 名词](#7-核心心法动词--数字--名词)
8. [进阶：可视模式、宏、多文件](#8-进阶可视模式宏多文件)
9. [插件：装这 5 个就够了](#9-插件装这-5-个就够了)
10. [一份够用的 vimrc 模板](#10-一份够用的-vimrc-模板)
11. [速查表（打印出来贴墙上）](#11-速查表)
12. [7 天练习路线](#12-7-天练习路线)

---

## 1. 先想明白：为什么学 Vim

一句话：**Vim 是唯一保证「任何一台 Linux 服务器上都有」的编辑器。**

- 改服务器上的配置、看线上日志、写 commit message、进容器排查问题——没有 VS Code，只有 vi/vim。
- 模式化编辑一旦肌肉记忆形成，改字基本不用碰鼠标和方向键，手不离主键位。

**给你的两个建议（能少走 80% 弯路）：**

1. 前 30 分钟先在终端跑 `vimtutor`（自带官方 30 分钟教程，中文版），比看任何文章都快。
2. 前两周**强制自己**用 Vim 写东西，难受就难受——肌肉记忆没有捷径。想回 VS Code 就回，别硬扛到崩溃。

---

## 2. 第一性原理：模式（这是 Vim 最大的坎）

新手 90% 的痛苦来自一件事：**Vim 不是「打字机」，而是「指挥官 + 打字员」两个角色。**

| 模式 | 大白话 | 进去方式 | 出来方式 |
|---|---|---|---|
| **Normal（普通）** | 指挥官：发号施令（移动、删除、复制），**默认模式** | 启动就是 / 其他模式按 `Esc` | — |
| **Insert（插入）** | 打字员：老老实实打字，此时才像记事本 | 按 `i` `a` `o` 等 | `Esc` |
| **Visual（可视）** | 先用光标选中，再对选中区域动手 | 按 `v` `V` `Ctrl+v` | `Esc` |
| **Command（命令行）** | 敲冒号命令：保存、替换、退出 | 按 `:` | `Esc` |

```
                      i / a / o（要打字了）
        ┌────────────────────────────────┐
        │                                ▼
   ┌─────────┐                     ┌─────────┐
   │ Normal  │                     │ Insert  │
   │ 指挥官   │                     │ 打字员   │
   └─────────┘                     └─────────┘
     ▲  │  ▲      Esc 一键回家（所有模式都按它） ▲
     │  │  │                                  │
     │  │  └──── v / V / Ctrl+v ──┐          │
     │  │                         ▼          │
     │  │                   ┌─────────┐      │
     │  └── : ──▶ Command │ Visual  │──────┘
     │      （:w :q :%s）  └─────────┘   Esc
     │
     └── 启动 vim 时永远站在 Normal 模式
```

**三条铁律：**

1. **打完字立刻按 `Esc` 回 Normal**。Normal 才是家，Insert 只是短暂出差。
2. **任何时候懵了，狂按两下 `Esc`**，一定回家，不会坏。
3. **方向键能用但别用**，强迫自己用 `h j k l`（左手食指中指无名指的位置），一周就成肌肉记忆。

> 为什么默认不是打字模式？因为编辑 = 移动 + 修改，而移动的时间远多于打字。Vim 把「移动和下指令」设为默认，就是让你改字比打字快。

---

## 3. 第 1 天必会：打开、改、存、退

```bash
vim file.txt        # 打开文件（没有就新建）
vim +42 file.txt    # 打开并跳到第 42 行
```

进去了以后，第一天只需要会这一套：

| 按键 | 意思 | 记法 |
|---|---|---|
| `i` | 开始打字 | **i**nsert |
| `Esc` | 打完字回来 | — |
| `:w` | 保存 | **w**rite |
| `:q` | 退出 | **q**uit |
| `:wq` 或 `ZZ` | 保存并退出 | — |
| `:q!` | **不保存**强退（改乱了就跑） | quit, 强制 |
| `:e!` | 放弃当前修改，恢复到上次保存 | edit, 强制 |

> 实例：改一个配置文件
> `vim nginx.conf` → `i` → 改字 → `Esc` → `:wq`。第一天会这些就能干活了。

**保命须知**：Vim 有 undo，而且默认配置下没保存就退出会留 `.swp` 交换文件。看到 `E325: ATTENTION` 别慌，一般按 `D`（删除交换文件继续）或 `R`（恢复）即可，本文的 vimrc 模板已帮你配好持久化 undo，以后基本不会再遇到。

---

## 4. 移动：先把路走熟

### 4.1 单位越小越常用，先记前三个

| 按键 | 作用 | 记法 |
|---|---|---|
| `h j k l` | 左 / 下 / 上 / 右 | j 向下（钩子往下），k 向上 |
| `w` / `b` | 下一个词开头 / 上一个词开头 | **w**ord / **b**ack |
| `e` / `ge` | 下一个词结尾 / 上一个词结尾 | **e**nd |
| `0` / `$` | 行首硬头 / 行尾 | 0 列数，$ 美元甩到尾 |
| `^` / `g_` | 行首第一个非空字符 / 行尾最后一个非空字符 | — |
| `gg` / `G` | 文件头 / 文件尾 | — |
| `5G` 或 `:5` | 跳到第 5 行 | 配合相对行号超好用 |
| `{` / `}` | 上一个 / 下一个空行（按段落跳） | — |

### 4.2 屏内滚动

| 按键 | 作用 |
|---|---|
| `Ctrl+d` / `Ctrl+u` | 向下 / 向上翻半屏（最常用） |
| `Ctrl+f` / `Ctrl+b` | 翻一整屏 |
| `zz` | 当前行滚到屏幕**中间**（改代码时钉住上下文，强烈推荐） |
| `zt` / `zb` | 当前行顶到屏幕顶 / 底 |

### 4.3 行内精准跳（杀器）

| 按键 | 作用 | 例子 |
|---|---|---|
| `f{字符}` | 行内向右跳到该字符上 | `f"` 跳到下一个引号上 |
| `F{字符}` | 行内向左跳 | — |
| `t{字符}` | 跳到该字符**前**一格 | **t**ill |
| `;` / `,` | 重复 / 反向重复上一次 f/t | 找第 3 个引号：`f";;;` |
| `%` | 在配对括号 `()[]{}` 间来回跳 | 光标在 `(` 上按 `%` 跳到配对的 `)` |
| `*` / `#` | 跳到下一个 / 上一个**相同单词** | 光标在变量上按 `*`，全文件找它 |
| `Ctrl+o` / `Ctrl+i` | 跳回上一个 / 下一个位置（像浏览器后退） | `G` 跳到文件尾看完后 `Ctrl+o` 弹回来 |

---

## 5. 编辑：增删改复制粘贴

### 5.1 进入打字模式的 6 种姿势（重点）

光标在哪、想从哪打字，决定按哪个：

| 按键 | 效果 | 记法 | 典型场景 |
|---|---|---|---|
| `i` | 光标**前**插入 | insert | 最普通 |
| `a` | 光标**后**插入 | append | 光标停在行尾标点上 |
| `I` | 行**首**插入 | 大写=加强 | 行头加 `#` 注释 |
| `A` | 行**尾**插入 | | 行尾加分号，比 `$i` 快 |
| `o` | **下方**新建一行 | open | 最常用之一 |
| `O` | **上方**新建一行 | | — |

### 5.2 删（删了不进垃圾篓，进寄存器，能贴回来）

| 按键 | 效果 |
|---|---|
| `x` | 删光标处 1 个字符 |
| `dd` | 删整行；`3dd` 删 3 行 |
| `D` 或 `d$` | 删到行尾 |
| `dw` | 删一个词（详见第 7 节「动词+名词」） |
| `dt(` | 删到下一个 `(` 之前（t = till） |
| `cc` / `S` | 清空整行并进入打字模式 |
| `cw` | 删一个词并进入打字模式（改词神器） |
| `C` | 删到行尾并进入打字模式 |

### 5.3 复制 / 粘贴 / 撤销

| 按键 | 效果 | 记法 |
|---|---|---|
| `yy` | 复制整行；`3yy` 复制 3 行 | yank（猛拽一下） |
| `yw` / `yiw` | 复制一个词 | 见第 7 节 |
| `p` / `P` | 粘贴到光标后 / 前 | paste |
| `xp` | 交换两个字符（p 隐含小技巧） | — |
| `ddp` | 上下两行交换 | — |
| `u` | 撤销 | undo |
| `Ctrl+r` | 重做（撤销你的撤销） | redo |

> **删/复制的都进了「寄存器」**，`dd` 是「剪切」，`p` 就能贴回来，别怕删错——`u` 万能。

### 5.4 大小写与大小写转换

| 按键 | 效果 |
|---|---|
| `~` | 光标处字符大小写互换 |
| `gUU` | 整行全大写；`guu` 整行全小写 |
| `gUiw` | 光标所在单词变大写 |

### 5.5 重复：`.` 命令（性价比最高的一个键）

`.` = **把上一次的修改原样再来一遍**。

> 实例：把一段文字里 5 个 `foo` 都改成 `bar`：
> `cwbar Esc`（改第一个）→ `n`（跳到下一个）→ `.`（重复改）→ `n` → `.` → …
> 你只「教」了 Vim 一次，剩下全是 `n .` 二连。

---

## 6. 查找与替换

### 6.1 查找

| 按键 | 作用 |
|---|---|
| `/关键词` | 向下找，`Enter` 确认 |
| `?关键词` | 向上找 |
| `n` / `N` | 下一个 / 上一个匹配 |
| `*` | 直接以光标下的单词查找（看代码时天天用） |
| `:noh` | 取消高亮（查找完清爽一点） |

### 6.2 替换：`:s` 命令（背下这一条就够了）

```
:[范围]s/旧/新/[标志]
```

| 命令 | 意思 |
|---|---|
| `:s/foo/bar/` | 只换**当前行**第一个 foo |
| `:s/foo/bar/g` | 换当前行**所有** foo（g = global，整行） |
| `:%s/foo/bar/g` | **整个文件**所有 foo |
| `:%s/foo/bar/gc` | 全文件替换，但每处**先问你**（c = confirm，改线上配置前用它） |
| `:5,12s/foo/bar/g` | 只换第 5–12 行 |

> 实例：把日志里的 `ERROR` 全换成 `WARN`，逐处确认：
> `:%s/ERROR/WARN/gc` → 每处按 `y` 确认、`n` 跳过、`a` 全部确认。

---

## 7. 核心心法：动词 + 数字 + 名词

这是 Vim 和其他编辑器**本质区别**的地方，想通了它，几百个快捷键你就不用背了。

Vim 的编辑命令像一句**可拼接的英文短句**：

```
   ┌─────────┐   ┌───────┐   ┌──────────────────┐
   │  动词    │ + │ 数字  │ + │  名词（作用对象）  │
   │ operator │   │ count │   │ motion/textobject │
   └─────────┘   └───────┘   └──────────────────┘
      做什么       做几次        对什么东西做
```

**动词**（operator）：`d` 删除、`c` 修改、`y` 复制、`gU` 变大写、`gu` 变小写、`>` 增加缩进、`<` 减少缩进

**名词**（motion / textobject）：

| 名词 | 意思 | 记法 |
|---|---|---|
| `w` | 一个词 | word |
| `$` `0` `^` | 到行尾 / 行首 | 复用第 4 节的移动键 |
| `f(` `t,` | 到某字符 / 到某字符前 | 复用 f/t |
| `iw` | **内部**单词（不含空格） | inner word |
| `aw` | 一个**完整**单词（含周围空格） | a word |
| `i"` / `a"` | 引号**里** / 引号**带引号** | inner/around quote |
| `i(` / `a(` | 括号里 / 括号带括号（`ib`/`ab` 同义） | inner/around parens |
| `ip` / `ap` | 一段 / 一段带空行 | inner/around paragraph |

### 组合起来读：

| 命令 | 大白话 |
|---|---|
| `dw` | 删一个词 |
| `d3w` | 删 3 个词 |
| `d$` | 删到行尾 |
| `ci"` | **改**引号**里**的内容（删掉旧的，直接开始打新的）——改字符串天天用 |
| `ca(` | 改整个括号块（连括号一起换掉） |
| `yi(` | 复制括号里的内容 |
| `diw` | 删光标处的单词（管光标在词中间哪里都行！） |
| `>ip` | 当前段落整体缩进 |
| `dt,` | 删到下一个逗号前 |

> **划重点**：`i`（inner）和 `a`（around）就是文本对象的灵魂——
> `diw` 你人站在词的**任何位置**都能删掉整个词；`ci"` 你站在引号内**任何位置**都能改整个字符串。不用再精确移动光标了。

配合 5.5 的 `.` 命令：`ci"新内容 Esc` 之后，跳到下一个字符串按 `.`，同样的修改立刻复制一份。**这套「修改一次 + n/. 二连」就是 Vim 日常干活的基本盘。**

---

## 8. 进阶：可视模式、宏、多文件

### 8.1 可视模式：先用眼睛选中，再动手

| 按键 | 选中范围 |
|---|---|
| `v` | 字符级（像鼠标拖选） |
| `V` | 整行（行级选中） |
| `Ctrl+v` | **列块**（矩形选区，Vim 特色） |

选中后直接接动词：`d` 删、`y` 复制、`c` 改、`:` 进命令行（自动填好 `'<,'>`，即「对选中范围生效」）、`u`/`U` 转小写/大写、`>` `<` 缩进。

**列块模式实例（批量操作）**——给 10 行行首统一加 `# `：

1. 光标到第一行行首，`Ctrl+v`
2. `9j`（向下选 10 行）
3. `I# ` （大写 I，输入 `# `）——注意此时**只有第一行**在变
4. `Esc` ——见奇迹：10 行全部生效

同理可以批量删行首 N 个字符（选中后按 `d`）、批量行尾追加（选中列后按 `A`，输入内容，`Esc`）。

### 8.2 宏：把一套动作录下来，批量重放

`q` = 录像机：`q{寄存器}` 开始录，再按 `q` 停，`@{寄存器}` 播放，`@@` 重放上一个。

> 实例：100 行数据，每行都要「跳到行尾、加个分号、再换行加注释」这种复杂重复：
>
> 1. 光标停第一行，`qa` 开始录制到寄存器 a
> 2. 做一遍操作：`A; Esc`（行尾加分号）→ `j`（到下一行）
> 3. `q` 停止录制
> 4. `99@a` —— 把剩下 99 行全部重放一遍
>
> 配合相对行号，`99@a` 里的次数直接数行号就行。

宏比列块更通用（列块只能加前缀后缀，宏可以录任意序列），是「重复劳动终结者」。

### 8.3 多文件：buffer / window / tab 到底是啥

大白话一张图：

```
  tab（标签页）  = 一张办公桌
  window（窗口） = 桌上的分屏格子，一格只能放一个 buffer
  buffer（缓冲） = 打开过的文件本体（都在内存里排队）
```

日常**最常用的是 window（分屏）和 buffer 切换**，tab 反而不常用：

| 按键/命令 | 作用 |
|---|---|
| `:e 文件` | 打开另一个文件 |
| `:ls` | 列出所有打开的 buffer |
| `:b 3` 或 `:b 文件名<Tab>` | 切到某个 buffer（Tab 补全很好用） |
| `Ctrl+^` | 在「当前 buffer 和上一个 buffer」之间来回切（看代码跳来跳去神器） |
| `:sp 文件` / `:vs 文件` | 水平 / 垂直分屏打开文件 |
| `Ctrl+w` 后接 `h j k l` | 在分屏间移动焦点 |
| `Ctrl+w` 后接 `H J K L` | 移动分屏位置 |
| `Ctrl+w =` | 所有分屏等分 |
| `Ctrl+w c` | 关掉当前分屏（buffer 还在） |
| `:Ex` | 内置文件浏览器（不装插件也能凑合看目录） |

---

## 9. 插件：装这 5 个就够了

**原则：初学阶段插件越少越好。** 插件是锦上添花，模式内功才是本体。下面 5 个都是「纯 Vimscript、服务器上也能用、不挑环境」的经典款。

### 9.1 先装插件管理器 vim-plug（一行命令）

```bash
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
  https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
```

以后所有插件只需要在 vimrc 里加一行 `Plug '作者/插件名'`，然后进 Vim 执行 `:PlugInstall`。

### 9.2 五个必装插件

| 插件 | 解决什么问题 | 日常怎么用 |
|---|---|---|
| `scrooloose/nerdtree` | 左侧目录树 | `\nt` 打开；装完记得加映射（见模板） |
| `preservim/nerdcommenter` | 一键注释/反注释任意语言 | `\c<空格>`：当前行/选中行 切换注释 |
| `tpope/vim-surround` | 增删改引号、括号、标签等「包裹」 | `cs"'`：把双引号换成单引号；`ds"`：去掉引号；`ysiw]`：给词套上方括号 |
| `airblade/vim-gitgutter` | 行旁实时显示 git 增删改标记 | `]c` / `[c` 跳到下一处/上一处改动 |
| `itchyny/lightline.vim` | 好看的状态栏（显示模式/文件/行号） | 装上就行，零配置能用 |

> 可选第 6 个：`junegunn/fzf.vim`（模糊搜文件/全文内容，需要额外装 fzf 二进制）。服务器上找文件很爽，但先把上面 5 个用熟。

### 9.3 安装演示

vimrc 里写上（模板已含）：

```vim
call plug#begin('~/.vim/plugged')
Plug 'scrooloose/nerdtree'
Plug 'preservim/nerdcommenter'
Plug 'tpope/vim-surround'
Plug 'airblade/vim-gitgutter'
Plug 'itchyny/lightline.vim'
call plug#end()
```

然后 `vim` → `:PlugInstall` → 完成。

### 9.4 没提的插件，先别装

- **补全/LSP 类**（coc.nvim、YouCompleteMe）：重、依赖多、服务器上编译费劲。初期用 Vim 自带 `Ctrl+n` 补全（打字时按，弹出当前文件里的词）就够，想升级再研究 Neovim + 内置 LSP。
- **配色/花活类**：先解决生产力，再考虑化妆。

---

## 10. 一份够用的 vimrc 模板

放在 `~/.vimrc`，每一行都写了注释，想改哪行直接改：

```vim
" ============ 基础体验 ============
syntax on                    " 语法高亮
set number                   " 显示行号
set relativenumber           " 相对行号（配合 5G、d3j 等数字跳转，神器）
" 两个同时开：当前行显示绝对行号，其他行显示相对行号
set cursorline               " 高亮当前行
set showcmd                  " 右下角显示你正在敲的命令
set wildmenu                 " 命令行 Tab 补全菜单
set scrolloff=5              " 光标上下至少留 5 行，别贴边
set laststatus=2             " 永远显示状态栏

" ============ 搜索 ============
set hlsearch                 " 高亮搜索结果
set incsearch                " 边打字边高亮
set ignorecase               " 搜索忽略大小写
set smartcase                " 但含大写时精确匹配（两个一起开才生效）

" ============ 缩进 ============
set tabstop=4                " Tab 显示宽度 4
set shiftwidth=4             " 缩进宽度 4
set expandtab                " Tab 转空格
set autoindent               " 新行继承缩进

" ============ 其他 ============
set backspace=indent,eol,start " 让退格键像正常编辑器一样能删
set undofile                 " 持久化撤销：关掉文件重开还能 u 回去
set undodir=~/.vim/undodir   " undo 存这里（目录要存在，见下）
set updatetime=300           " gitgutter 等插件刷新更快
set mouse=a                  " 支持鼠标（滚轮翻页、点选），新手友好
set encoding=utf-8
let mapleader=" "            " 把 \ 换成空格当Leader键（插件组合键的前缀）

" ============ 快捷键 ============
nnoremap <leader>nt :NERDTreeToggle<CR>   " 空格 nt 开关目录树
nnoremap <leader>w :w<CR>                 " 空格 w 保存
vnoremap <C-c> "+y                        " 可视模式下 Ctrl+C 复制到系统剪贴板
nnoremap <C-v> "+p                        " Ctrl+V 粘贴系统剪贴板
noremap <C-h> :noh<CR>                    " Ctrl+H 取消搜索高亮

" ============ 插件 ============
call plug#begin('~/.vim/plugged')
Plug 'scrooloose/nerdtree'
Plug 'preservim/nerdcommenter'
Plug 'tpope/vim-surround'
Plug 'airblade/vim-gitgutter'
Plug 'itchyny/lightline.vim'
call plug#end()
```

配套一次性操作：

```bash
mkdir -p ~/.vim/undodir     # undo 目录需要手动建一次
vim ~/.vimrc                # 粘贴上面的配置
# 进 vim 后执行 :PlugInstall 装插件
```

> 改完 vimrc 用 `:source ~/.vimrc` 立即生效，不用重启。
> **注意**：Leader 键改成了空格后，nerdcommenter 的注释键从 `\c<空格>` 变成 `<空格>c<空格>`。

---

## 11. 速查表

### 救命三键

```
Esc        —— 任何模式回 Normal
u          —— 撤销
:q!        —— 不保存跑路
```

### 移动

```
h j k l     左下上右        w / b      词前进/后退
0 / $       行首/行尾       gg / G     文件头/尾
f{c} + ;    行内找字符      *          找光标单词
Ctrl+d/u    翻半屏          zz         当前行居中
Ctrl+o      跳回上一位置
```

### 编辑

```
i a I A o O  六种进入打字   Esc        回家
dd / yy / p  删行/复行/贴   x          删字符
cw / C       改词/改到行尾  u / Ctrl+r 撤销/重做
.            重复上次修改   ~          大小写互换
```

### 句子公式（动词+数字+名词）

```
d  c  y      删/改/复制     iw aw      词内部/整词
i" a"        引号里/带引号  i( a(      括号里/带括号
ci"          改字符串       diw        删单词
>ip          段落缩进       dt,        删到逗号前
```

### 查找替换 / 批量

```
/n   N       查找/下一个     :%s/旧/新/gc   全文替换带确认
v V Ctrl+v   三种选中        qa...q @a      宏录制/播放
:sp :vs      水平/垂直分屏   Ctrl+w hjkl   分屏跳转
:b 名称Tab   切buffer        Ctrl+^        上个文件来回切
```

---

## 12. 7 天练习路线

| 天 | 任务 | 达标标准 |
|---|---|---|
| 1 | 跑完 `vimtutor`；用本文 3–4 节 | 能打开/改/存/退，能用 hjkl 移动 |
| 2 | 强制用 Vim 写任何文字 | 记熟 i a I A o O 六种进入方式 |
| 3 | 用 Vim 改一个真实配置文件 | 会 dd yy p u，会 :wq :q! |
| 4 | 刻意练习 `/` `*` `n` `:%s` | 能完成一次全文替换 |
| 5 | 练「动词+名词」：diw ci" daw ca( | 改代码字符串/括号内容不再移动光标 |
| 6 | 装好 vimrc + 5 插件；练 v / Ctrl+v 列块 | 能批量注释、批量加前缀 |
| 7 | 练宏和 `.` 命令 | 能用 `qa…q@a` 处理一次 10 行以上的重复编辑 |

之后就是日常使用自然变强。遇到不会的操作：**Vim 内按 `:help 关键词` 查文档**（比如 `:help text-objects`），这是最好的手册。

---

*生成于 2026-09-22，基于 Tavily 检索的社区实践（vim-plug / nerdcommenter / vim-surround / vim-gitgutter / lightline 均为长期维护的经典插件）。*
