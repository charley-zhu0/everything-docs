# Yazi 终端文件管理器使用指南（写给初学者）

> 目标：不追求全功能，读完就能把 yazi 当日常文件管理工具用。
> 姊妹篇：[`docs/vim-usage.md`](./vim-usage.md)。yazi 键位大量借鉴 Vim，那边学过的这边直接白捡。
> 全文讲法：大白话 + 图 + 例子。所有例子都可以直接开着 `y` 照着敲。

---

## 目录

1. [先想明白：有 vim 了还要文件管理器干嘛](#1-先想明白有-vim-了还要文件管理器干嘛)
2. [安装（本机已装好，这里说清楚坑）](#2-安装本机已装好这里说清楚坑)
3. [第 1 天必会：启动、看图、退出](#3-第-1-天必会启动看图退出)
4. [移动与目录导航](#4-移动与目录导航)
5. [文件操作：选择、复制、移动、删除、重命名](#5-文件操作选择复制移动删除重命名)
6. [查找、过滤、排序](#6-查找过滤排序)
7. [进阶：任务队列、跑 shell、多标签](#7-进阶任务队列跑-shell-多标签)
8. [可选依赖与本机现状](#8-可选依赖与本机现状)
9. [配置入门](#9-配置入门)
10. [速查表](#10-速查表)
11. [常见问题](#11-常见问题)
12. [3 天练习路线](#12-3-天练习路线)

---

## 1. 先想明白：有 vim 了还要文件管理器干嘛

分工不同：**vim 管「改文件内容」，yazi 管「文件本身」**——找文件、看大小、批量移动复制、翻目录、预览图片/压缩包/代码。

yazi 是 Rust 写的终端文件管理器，最大卖点：**所有操作异步执行**。复制 10GB 的目录，界面照常滑动不卡顿，任务后台跑。

什么时候用它：

- 不知道文件在哪，想一层层翻着找（比 `ls` + `cd` 直观得多）
- 批量整理文件：全选 → 剪切 → 换目录粘贴，三步搞定
- 快速看一眼：代码高亮、图片缩略图、压缩包内容、JSON 格式化，不用打开
- 看完目录还想把 shell 留在那个目录（`y` 启动，退出自动 cd）

---

## 2. 安装（本机已装好，这里说清楚坑）

**本机（Ubuntu 22.04）已完成：**

| 组件 | 位置 | 说明 |
|---|---|---|
| `yazi` + `ya` v26.9.1 | `~/.local/bin/`（已在 PATH） | musl 静态编译版 |
| zsh 补全 | `~/.oh-my-zsh/custom/completions/_y{azi,a}` | Tab 补全命令参数 |
| `y` 包装函数 | `~/.zshrc` 末尾 | 退出时 shell 自动 cd（见第 3 节） |

> **踩过的坑（换机器时必看）**：官方最新版预编译包在本机跑不起来——它要求 GLIBC ≥ 2.39，Ubuntu 22.04 只有 2.35。解决办法是下载 **musl 静态版**（文件名带 `musl` 的那个），静态链接不挑 glibc，任何 Linux 都能跑。服务器上的老系统同理。
>
> ```bash
> # 通用安装姿势（老系统也适用）
> curl -fsSL -o /tmp/yazi.zip https://github.com/sxyazi/yazi/releases/latest/download/yazi-x86_64-unknown-linux-musl.zip
> unzip /tmp/yazi.zip && cp yazi-x86_64-unknown-linux-musl/{yazi,ya} ~/.local/bin/
> ```

其他机器装完别忘了三件事：`~/.local/bin` 进 PATH、复制 zsh 补全、zshrc 里加 `y` 包装函数（官方 Quick Start 有现成的）。

---

## 3. 第 1 天必会：启动、看图、退出

```bash
yazi      # 直接启动（退出后 shell 还在原目录）
y         # 推荐用这个启动（包装函数），退出后 shell 停在你浏览的目录
```

进去看到的是三栏布局，大白话就是「上一级 | 当前 | 预览」：

```
 ┌────────────┬─────────────────────────┬──────────────────┐
 │  上一级     │  当前目录                 │  预览             │
 │            │                         │                  │
 │  docs/     │  ▶ adr/                 │  （选中内容的      │
 │  src/      │    agent-tool.md  12K   │    内容/缩略图/    │
 │  CLAUDE.md │    issue-tracker.md 3K  │    摘要）          │
 │            │    vim-usage.md    18K  │                  │
 └────────────┴─────────────────────────┴──────────────────┘
      你永远在中间那栏操作，h 往左走 l 往右走
```

第一天只需要会这一套：

| 按键 | 意思 | 记法 |
|---|---|---|
| `j` / `k` | 移动光标（和 vim 一样） | — |
| `l` / `Enter` | 进入目录 / 打开文件 | — |
| `h` | 回到上一级目录 | — |
| `q` | 退出（配合 `y` 启动，shell 会 cd 到当前浏览的目录） | — |
| `Q` | 退出，但 shell 停在原地 | 大写 = 强硬版 |
| `~` 或 `F1` | **打开帮助（忘了任何按键按它）** | — |

> `y` 包装函数的原理一句话：启动时给 yazi 一个临时文件，你在里面每换一个目录它就往文件里写路径，退出后函数读出来执行 `cd`。所以 `y` 进去 → 翻到想去的目录 → `q` 退出 → shell 已经站在那里了。不想改目录就 `Q` 退出。

---

## 4. 移动与目录导航

### 4.1 列表内移动（和 vim 几乎一样）

| 按键 | 作用 |
|---|---|
| `j` / `k` | 下 / 上 |
| `gg` / `G` | 顶部 / 底部 |
| `Ctrl+u` / `Ctrl+d` | 上 / 下翻半页 |
| `Ctrl+b` / `Ctrl+f` | 上 / 下翻整页 |
| `J` / `K` | 滚动**预览栏**（看大文件时用） |

### 4.2 目录间跳转

| 按键 | 作用 | 记法 |
|---|---|---|
| `h` / `l` | 上级 / 进入 | 和 vim 的左右一致 |
| `H` / `L` | 目录历史：后退 / 前进（像浏览器） | 大写 = 加强 |
| `gh` | 回 home | goto home |
| `g` + `空格` | 交互式输路径跳转 | — |
| `g` `t` | 打开回收站 | goto trash |
| `z` | 模糊搜索跳转（需要装 fzf） | — |
| `Z` | 智能跳目录（需要装 zoxide，会记住你去过的目录） | — |

### 4.3 两个好习惯

1. **多用 `H` 后退**而不是 `h` 一层层爬回来，历史跳转快得多。
2. 预览栏看长文件时，`J`/`K` 滚动，比进出文件快。

---

## 5. 文件操作：选择、复制、移动、删除、重命名

### 5.1 先选中（yazi 的所有操作都作用于「选中项」，没有选中就作用于光标所在项）

| 按键 | 作用 |
|---|---|
| `空格` | 选中/取消当前项，并跳到下一行（连续选多个就连续按） |
| `v` | 可视模式：`j`/`k` 划过的全部选中（和 vim 的 V 一个手感） |
| `Ctrl+a` | 全选 |
| `Ctrl+r` | 反选 |
| `Esc` | 清空所有选择 |

### 5.2 复制 / 移动 / 粘贴（标准流程：选中 → yank → 去目标目录 → 粘贴）

| 按键 | 作用 | 记法 |
|---|---|---|
| `y` | 复制（yank） | 和 vim 一样 |
| `x` | 剪切 | — |
| `p` | 粘贴 | paste |
| `P` | 粘贴并覆盖同名文件 | 大写 = 强硬版 |
| `Y` 或 `X` | 取消复制/剪切状态 | — |

> 实例：把 `logs/` 下所有 `.gz` 挪到 `archive/`：
> 进 logs → `.`（显示隐藏）不需要就跳过 → 按 `,` 无关 → `Ctrl+a` 全选不行（会带上其他文件），那就 `f` 过滤出 gz → `v` `Ctrl+a`（全选过滤结果）→ `x` 剪切 → `h` `h` 回去 → 进 archive → `p`。全程不敲一条命令。

### 5.3 删除（注意大小写区别！）

| 按键 | 作用 |
|---|---|
| `d` | 移入**回收站**（可找回，日常用它） |
| `D` | **永久删除**（大写 = 强硬版，小心按） |
| `g` `t` | 打开回收站找误删的文件 |

> 和 vim 的记忆规则一致：**大写键都是更狠的版本**。日常删东西永远用小写 `d`。

### 5.4 新建 / 重命名

| 按键 | 作用 |
|---|---|
| `a` | 新建文件；**名字以 `/` 结尾就建目录**（如 `a` 输入 `docs/`） |
| `A` | 批量新建多个 |
| `r` | 重命名，**光标自动停在扩展名前**（改名不改后缀，贴心） |

### 5.5 复制路径（写文档、贴路径神器）

`c` 前缀系列：

| 按键 | 复制什么 |
|---|---|
| `c` `c` | 完整绝对路径 |
| `c` `f` | 仅文件名 |
| `c` `n` | 文件名（不含扩展名） |
| `c` `d` | 所在目录路径 |

复制到的是 yazi 自己的寄存器（配合第 7 节的 `;` 直接用），想进系统剪贴板在 shell 里 `xclip`/`wl-copy` 或直接用 `;` 里回显。

---

## 6. 查找、过滤、排序

| 按键 | 作用 | 说明 |
|---|---|---|
| `f` | **过滤**：边打字边把当前目录筛剩匹配项（当前目录内） | filter |
| `/` | **查找**：光标跳到下一个匹配文件名，`n`/`N` 下一个/上一个（和 vim 一样） | find |
| `s` | **搜索**：递归搜全子目录的文件名（需要 `fd`） | search |
| `S` | **内容搜索**：按文件内容搜（如搜哪个文件里有 `TODO`，需要 `rg`，本机已有） | — |
| `Ctrl+s` | 取消正在进行的搜索 | — |

> `f` 和 `/` 的区别大白话：`f` 是「把不相干的藏起来，剩下的我来挑」；`/` 是「帮我跳到那个文件上」。

### 排序（`,` 前缀，大写 = 倒序）

| 按键 | 排序方式 |
|---|---|
| `,` `s` / `,` `S` | 按大小正 / 倒序（找大文件清理磁盘，`,S` 一秒定位） |
| `,` `m` / `,` `M` | 按修改时间正 / 倒序（找最近改过的文件） |
| `,` `a` | 按字母 |
| `,` `r` | 随机打乱（嗯，偶尔有用） |

### 列表信息模式（`m` 前缀）

默认列表只显示名字，按 `m` + 一位字母切换每行右侧显示什么：`mm` 修改时间、`ms` 大小、`mo` 属主、`mp` 权限。

---

## 7. 进阶：任务队列、跑 shell、多标签

### 7.1 `w`：任务管理器（yazi 的灵魂功能）

所有复制/剪切/删除都是后台任务。大目录复制时界面不卡，按 `w` 打开任务面板看进度：`j/k` 选任务、`Enter` 看详情、`x` 取消、`w`/`Esc` 关面板。

### 7.2 `;` 和 `:`：不退出 yazi 跑 shell 命令

| 按键 | 作用 |
|---|---|
| `;` | 弹出输入行跑命令，`%s` = 选中文件（占位符） |
| `:` | 同上，但阻塞等命令跑完（看输出） |

> 实例：选中三个日志文件，按 `:` 输入 `wc -l %s` → 回车，直接看每个文件行数，看完还在 yazi 里。
> 这就是 5.5 复制路径的正确用法：`cc` 复制路径 → `;` → 粘贴到你的命令里。

### 7.3 `Tab`：spot 文件详情

光标停在文件上按 `Tab`，弹出详情面板（大小、权限、时间、哈希、文本摘要……），`h`/`l` 可以在相邻文件间滑动对比，再按 `Tab`/`Esc` 关闭。

### 7.4 多标签页（`t` 前缀）

| 按键 | 作用 |
|---|---|
| `t` `t` | 新建标签（当前目录） |
| `1`–`9` | 切到第 N 个标签 |
| `[` / `]` | 上一个 / 下一个标签 |

> 典型用法：一个 tab 在 `src/` 挑文件，一个 tab 开着 `dist/` 收文件，两边来回切着搬。

### 7.5 打开方式

| 按键 | 作用 |
|---|---|
| `o` / `Enter` | 按默认规则打开：文本文件用 `$EDITOR`（即 vim），目录进入，图片看终端支持 |
| `O` | 弹菜单让你挑打开方式 |

> 想让文本文件用别的编辑器打开：`export EDITOR=vim` 写进 zshrc 即可（yazi 默认回退到 vi）。

---

## 8. 可选依赖与本机现状

yazi 核心功能零依赖就能用（浏览、增删改、文本预览都行），下面这些装了体验更好。**本机现状**：

| 依赖 | 干什么用 | 对应按键/功能 | 本机 |
|---|---|---|---|
| `rg`（ripgrep） | 按内容搜文件 | `S` | ✅ 已有 |
| `jq` | JSON 预览格式化 | 预览栏 | ✅ 已有 |
| `fd` | 按文件名递归搜索 | `s` | ❌ 未装 |
| `fzf` | 模糊跳转 | `z` | ❌ 未装 |
| `zoxide` | 智能目录跳转 | `Z` | ❌ 未装 |
| `7-Zip`（7zz） | 压缩包预览/解压 | 预览、右键解压 | ❌ 未装 |
| `poppler`（pdftotext） | PDF 预览 | 预览栏 | ❌ 未装 |
| `ffmpeg` | 视频缩略图 | 预览栏 | ❌ 未装 |

想补齐（需要 sudo，可选）：

```bash
sudo apt install fd-find fzf p7zip-full poppler-utils ffmpeg
sudo ln -s $(which fdfind) /usr/local/bin/fd   # Ubuntu 22.04 的 fd 命令叫 fdfind，yazi 认 fd
# zoxide 建议按官方脚本装：curl -sSf https://raw.githubusercontent.com/ajeetdsouza/zoxide/main/install.sh | sh
```

> **不用 sudo 的替代**：`rg`、`jq` 已覆盖最常用的两个。fd/fzf/zoxide 装不上也不影响主流程，`/`、`f`、`H` 够用了。
> 排障神器：`ya env` 会打印环境和每个依赖/配置的检测结果，哪个缺了一目了然。

---

## 9. 配置入门

配置文件在 `~/.config/yazi/`，不建也能用（全默认）。想调，建一个 `yazi.toml` 只写想改的项，其余自动继承默认：

```toml
# ~/.config/yazi/yazi.toml
[mgr]
ratio = [ 1, 3, 4 ]      # 三栏宽度比：父/当前/预览（预览栏给大点）
linemode = "size"        # 列表每行直接显示文件大小
sort_dir_first = true    # 目录排前面（默认已开）
show_hidden = false      # 默认不显示隐藏文件

[preview]
wrap = "yes"             # 预览长行自动换行（读代码舒服）
tab_size = 4
```

另外两个文件按需再加：

- `keymap.toml`：改/加按键。官方建议的方式是覆盖式追加，比如把 `Ctrl+f` 从翻页改成别的——初学阶段不用动。
- `theme.toml` + flavors：换主题配色。锦上添花，最后再玩。

> 修改配置后 yazi 下次启动自动生效；正在跑的实例按 `q` 退出重进即可。

---

## 10. 速查表

### 救命三键

```
Esc        —— 清空选择 / 取消搜索 / 退出可视
q          —— 退出（shell 跟随到当前目录）
Q          —— 退出（shell 不动）
~ / F1     —— 任何按键忘了就按它
```

### 移动

```
h / l       上级/进入        j / k      下/上
gg / G      顶/底            Ctrl+d/u   翻半页
H / L       历史 后退/前进    gh         回 home
J / K       滚动预览栏        g 空格     输路径跳转
```

### 操作

```
空格 / v    选一个/划选       Ctrl+a / Ctrl+r   全选/反选
y / x / p   复制/剪切/粘贴    P         粘贴覆盖
d / D       进回收站/永久删    g t       打开回收站
a / r       新建/重命名       .         切换隐藏文件
c c         复制完整路径       c f       复制文件名
```

### 找与看

```
f           当前目录过滤      /  n N     查找并跳转
s / S       搜文件名/搜内容   Tab        spot 文件详情
, s / , m   按大小/时间排序    m s/m m    列表显示大小/时间
w           任务队列          ; / :      跑 shell 命令
t t / 1-9   新标签/切标签      o / O      打开/选方式打开
```

---

## 11. 常见问题

**Q1：图标全是方块 □？**
终端字体不是 Nerd Font。装一个（如 JetBrainsMono Nerd Font）并让终端用它即可；服务器上没有条件就忍着，不影响功能。

**Q2：预览栏想看图片？**
取决于终端：iTerm2、Kitty、WezTerm 等支持图形协议的终端直接出图；普通终端可以装 `chafa` 或 `ueberzugpp` 兜底。纯 SSH 场景看不了就算了，别折腾。

**Q3：按 `d` 删错了怎么办？**
`d` 只是进回收站：`g t` 打开回收站，选中按恢复即可（或 `ya pub trash-restore`）。只有 `D` 才是真删。

**Q4：`q` 退出后 shell 没跟着换目录？**
你大概是用 `yazi` 直接启动的。用 `y`（包装函数）启动才有退出跟随目录的效果——函数已经在 `~/.zshrc` 里了，新开终端生效。

**Q5：装最新版报 `GLIBC_x.xx not found`？**
下载文件名带 `musl` 的静态版（见第 2 节），任何 Linux 通用。

**Q6：和 vim 的按键冲突吗？**
不冲突——yazi 是独立程序，里面的 vim 键位是仿的，两套肌肉记忆互相增益。

---

## 12. 3 天练习路线

| 天 | 任务 | 达标标准 |
|---|---|---|
| 1 | 用 `y` 启动，随便翻目录，练 h/j/k/l、gg/G、H 后退，`q` 退出看 shell 是否跟随 | 能全程不碰鼠标翻到任意目录并退出 |
| 2 | 练文件操作：空格多选 → `x` → 换目录 `p`；`a` 建目录、`r` 重命名、`d` 删再从回收站找回 | 完成一次「整理下载目录」实战 |
| 3 | 练 `f` 过滤、`/` 查找、`,S` 按大小排序、`w` 看任务、`;` 跑命令 | 大目录里 10 秒内定位到最大的 3 个文件 |

之后就是日常自然变强。忘了按键第一反应：**按 `~` 查帮助**，比翻文档快。

---

*生成于 2026-09-22。yazi v26.9.1，键位取自官方默认 keymap（v26.9.1 tag），基于 Tavily 检索的官方文档与社区实践。*
